# ClawPlaza Server (Prototype)

This is a minimal server prototype for ClawPlaza.

## Run (development)

Install deps:

```
cd server
npm install
```

Run:

```
npm start
```

It starts an Express + Socket.io server with a small SQLite database.
